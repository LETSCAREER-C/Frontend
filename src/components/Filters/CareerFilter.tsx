// import { useState } from 'react';

// const items = ['All', '커리어 탐색', '서류 준비', '면접 준비'];

// const CareerFilter = () => {
//   const [activeItem, setActiveItem] = useState('All');

//   return (
//     <nav className="flex space-x-4">
//       {items.map(item => (
//         <button
//           key={item}
//           onClick={() => setActiveItem(item)}
//           className={`px-3 py-2 ${activeItem === item ? 'bg-blue-500 text-white' : 'bg-gray-200'}`}
//         >
//           {item}
//         </button>
//       ))}
//     </nav>
//   );
// };

// export default CareerFilter;
